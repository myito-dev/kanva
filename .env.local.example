# Copy to .env.local and fill in your own key to enable the "IA" analysis mode.
# Gemini has a free usage tier: https://aistudio.google.com/apikey
# Without a key, Kanva falls back automatically to the local rule-based analysis.
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.0-flash
